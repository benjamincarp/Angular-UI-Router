# Angular-UI-Router
simple app to play with UI Router in Angular1

## Setup
```
npm install 
cd /client  
bower install

```

## Run
```
node app.js
```
The app will now be available at localhost:3000/#!/